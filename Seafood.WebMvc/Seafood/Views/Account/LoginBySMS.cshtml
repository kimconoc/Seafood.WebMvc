
@{
    Layout = "~/Views/Account/_LayoutAccount.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://www.gstatic.com/firebasejs/9.12.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.12.1/firebase-auth-compat.js"></script>
    <style>
        .box-loginBySMS-form {
        }

            .box-loginBySMS-form .header {
                width: 100%;
                font-size: 25px;
            }


            .box-loginBySMS-form .box-input-login {
                margin-top: 20px;
                background-color: white;
                box-sizing: border-box;
                width: 100%;
                height: 45px;
                border: 1px solid rgba(0,0,0,.14);
                border-radius: 2px;
                width: 100%;
                display: flex;
                padding: 3px 10px 0 10px;
                overflow: hidden;
                box-shadow: inset 0 2px 0 rgb(0 0 0 / 2%);
                outline: none;
                justify-content: space-between;
            }

                .box-loginBySMS-form .box-input-login i {
                    width: auto;
                    font-size: 18px;
                    padding: 10px 0 8px 12px;
                    cursor: pointer;
                }

                .box-loginBySMS-form .box-input-login #show_password {
                    color: #05a;
                }

            .box-loginBySMS-form .btn-login {
                width: 100%;
                margin-top: 20px;
                height: 45px;
                font-size: 17px;
            }

            .box-loginBySMS-form .box-lable-login {
                margin-top: 5px;
                /*Chia đều khoảng và cái đứa cái cuối về sát lề*/
                display: flex;
                justify-content: space-between;
                color: #05a;
            }

                .box-loginBySMS-form .box-lable-login a {
                    color: #05a;
                }

            .box-loginBySMS-form .box-describe {
                margin-top: 20px;
                text-transform: uppercase;
                font-size: .75rem;
                display: flex;
            }

                .box-loginBySMS-form .box-describe span {
                    margin: 0 5px 0 5px;
                    font-size: 11px;
                }

                .box-loginBySMS-form .box-describe .slim-line {
                    margin-top: 7px;
                    height: 1.5px;
                    background-color: #ccc;
                }

            .box-loginBySMS-form .box-register {
                margin-top: 40px;
                align-items: center;
                display: flex;
                justify-content: center;
                width: 100%;
            }

                .box-loginBySMS-form .box-register a {
                    margin-left: 5px;
                    color: #ee4d2d;
                }
        #recaptcha-container{
            margin-top:5px;
        }
        /*Ghi đè class*/
        .box-loginBySMS-form .box-input-login input:focus {
            outline: none;
        }
    </style>
</head>
<body>
    <div class="box-loginBySMS-form">
        <div class="header">
            Đăng nhập
        </div>

        <div id="sender">
            <div id="box_login_form_account" class="box-input-login">
                <input id="number" name="Number" maxlength="12" placeholder="Số điện thoại">
            </div>
            <div id="recaptcha-container"></div>
            <button class="btn-login btn-core-accept" onclick="phoneAuth()">Gửi mã OTP</button>
        </div>
        <div id="verifier" style="display:none;">
            <div id="box_login_form_account" class="box-input-login">
                <input id="verificationcode" name="Verificationcode" maxlength="6" placeholder="OTP code">
            </div>
            <button class="btn-login btn-core-accept" onclick="codeverify()">Xác thực</button>
        </div>   
        <div class="box-lable-login">
            <a href=""></a>
            <a href="/Account/Login">Đăng nhập với mật khẩu</a>
        </div>
        <div class="box-describe">
            <div class="slim-line">
            </div>
            <span>Hoặc</span>
            <div class="slim-line">
            </div>
        </div>
        <div class="box-register">
            Bạn mới biết đến Seafood?
            <a href="">Đăng ký</a>
        </div>
    </div>
    <script>
        // For Firebase JS SDK v7.20.0 and later, measurementId is optional
        const firebaseConfig = {
            apiKey: "AIzaSyBw3O3gN08-rl7qkmXHzKpx2uGpJC2Db5M",
            authDomain: "yt-project-19f44.firebaseapp.com",
            projectId: "yt-project-19f44",
            storageBucket: "yt-project-19f44.appspot.com",
            messagingSenderId: "197151382206",
            appId: "1:197151382206:web:415ef57e57b7b73bd7dcef",
            measurementId: "G-E1RRMGEGDP"
        };
        // initializing firebase SDK
        firebase.initializeApp(firebaseConfig);
        // render recaptcha verifier
        render();
        function render() {
            window.recaptchaVerifier = new firebase.auth.RecaptchaVerifier('recaptcha-container');
            recaptchaVerifier.render();
        }
        // function for send OTP
        function phoneAuth() {
            var number = document.getElementById('number').value;
            firebase.auth().signInWithPhoneNumber(number,
                window.recaptchaVerifier).then(function (confirmationResult) {
                    window.confirmationResult = confirmationResult;
                    coderesult = confirmationResult;
                    document.getElementById('sender').style.display = 'none';
                    document.getElementById('verifier').style.display = 'block';
                }).catch(function (error) {
                    // error in sending OTP
                    alert(error.message);
                });
        }
        // function for OTP verify
        function codeverify() {
            var code = document.getElementById('verificationcode').value;
            var number = document.getElementById('number').value;
            coderesult.confirm(code).then(function () {
                executeVerified(number, "true");
                alert('Thành công');
            }).catch(function () {
                executeVerified(number, "false");
                alert('Thất bại');
            })
        }

        //Setup
        $(document).ready(function ()
        {
            if ('@ViewBag.Message' != '')
            {
                $('#alert_login').html('@ViewBag.Message');
                $('#alert_login').fadeIn(500);
                setTimeout("$('#alert_login').fadeOut(1500);", 5000 );
            };
        });
        $('#number').on('focus', function () {
            document.getElementById("box_login_form_account").style.borderColor = "rgb(7, 171, 226)";
        });
        $('#number').on('blur', function () {
            document.getElementById("box_login_form_account").style.borderColor = "rgba(0,0,0,.14)";
        });
        $('#verificationcode').on('focus', function () {
            document.getElementById("box_login_form_account").style.borderColor = "rgb(7, 171, 226)";
        });
        $('#verificationcode').on('blur', function () {
            document.getElementById("box_login_form_account").style.borderColor = "rgba(0,0,0,.14)";
        });
        //Setup
    </script>
</body>
</html>
